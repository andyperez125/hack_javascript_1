/**
 * mediante el loop for agregar los números 1,2,3,4,5
 * dentro del array result 
 * 
 * output => [1,2,3,4,5]
 */


let result = [];

for (let i = 1; i <= 5; i++) {
    result.push(i); // Agrega los números del 1 al 5 
}

// Exporta el array result
module.exports = result;